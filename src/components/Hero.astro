---
import DashboardPreview from "./DashboardPreview.astro";
import ArrowRight from "../assets/svg/arrow-right.astro";
---

<section
  id="hero"
  class="relative w-full min-h-screen bg-bg-main flex flex-col items-center justify-center overflow-hidden"
>
  <!-- Background Effects -->
  <div class="absolute inset-0 pointer-events-none overflow-hidden">
    <!-- Grid Pattern -->
    <div
      class="absolute inset-0 bg-grid opacity-[0.03] [mask-image:radial-gradient(ellipse_at_center,black,transparent_80%)]"
    >
    </div>

    <!-- Ambient Glows -->
    <div
      class="absolute top-[20%] left-[10%] w-[500px] h-[500px] bg-primary/10 blur-[120px] rounded-full animate-pulse-slow"
    >
    </div>
    <div
      class="absolute bottom-[20%] right-[10%] w-[600px] h-[600px] bg-primary/5 blur-[150px] rounded-full animate-float"
    >
    </div>

    <!-- Darkened Vignette -->
    <div
      class="absolute inset-0 bg-[radial-gradient(circle_at_center,transparent_0%,rgba(4,4,13,0.4)_100%)]"
    >
    </div>
  </div>

  <div class="relative z-10 max-w-4xl px-6 mt-32 md:mt-48 text-center">
    <!-- H1 – keyword-rich, benefit-led -->
    <h1
      class="font-serif text-4xl md:text-7xl text-text-primary tracking-tight leading-[1.5] md:leading-[1.1] mb-6"
    >
      #1 platform for <em class="text-primary">engineering documentation</em>
    </h1>

    <!-- Subtitle – problem/solution, secondary keywords -->
    <p
      class="text-text-secondary text-lg md:text-xl max-w-2xl mx-auto leading-relaxed mb-10"
    >
      Autonomous documentation for your codebase that evolves with every code
      change.
    </p>

    <!-- Primary CTA -->
    <div
      class="flex flex-col sm:flex-row items-center justify-center gap-4 my-10 md:my-8"
    >
      <a
        id="hero-cta-primary"
        href="https://app.penqwin.com"
        class="group select-none relative inline-flex items-center active:scale-95 justify-center gap-3 px-8 py-4 bg-white/[0.03] backdrop-blur-md border border-white/10 text-white rounded-full font-medium transition-all duration-500 hover:bg-white/[0.08] hover:border-primary/40 hover:shadow-[0_0_40px_-10px_rgba(139,92,246,0.3)]"
      >
        <span>Document Your Repo</span>
        <ArrowRight class="w-5 inline-block group-hover:translate-x-1" />
      </a>
    </div>
  </div>
  <div
    class="hidden md:block w-full md:w-2/3 max-w-5xl mt-12 md:mt-16 mb-20 md:mb-32 px-4 md:px-6 relative z-10"
  >
    <DashboardPreview />
  </div>
</section>
